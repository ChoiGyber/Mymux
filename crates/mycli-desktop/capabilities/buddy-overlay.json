{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "buddy-overlay",
  "description": "Desktop buddy overlay window: listens for show events and invokes app commands; window placement/visibility is driven by app commands on the Rust side.",
  "windows": ["buddy-overlay"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
